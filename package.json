{
	"name": "single-board-player",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"bro": "BROWSER=firefox vite dev --open",
		"dev": "vite dev",
		"build": "vite build",
		"deploy:frontend": "./scripts/deploy-frontend.sh",
		"deploy:assistant": "./scripts/deploy-backend.sh assistant",
		"deploy:playlists": "./scripts/deploy-backend.sh playlists",
		"deploy:backend": "./scripts/deploy-backend.sh",
		"deploy": "npm run deploy:frontend && npm run deploy:backend",
		"git:local": "read -p 'Суть коммита: ' COMMIT; git add . ; git commit -am $COMMIT",
		"git:remote": "git push origin svelte",
		"git": "npm run git:local && npm run git:remote"
	},
	"devDependencies": {
		"@sveltejs/adapter-auto": "^7.0.1",
		"@sveltejs/kit": "^2.63.0",
		"@sveltejs/vite-plugin-svelte": "^7.1.2",
		"svelte": "^5.56.1",
		"svelte-check": "^4.6.0",
		"typescript": "^6.0.3",
		"vite": "^8.0.16"
	},
	"dependencies": {
		"@sveltejs/adapter-static": "^3.0.10"
	}
}
